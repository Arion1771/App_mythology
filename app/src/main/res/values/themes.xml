<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Base.Theme.App_mythology" parent="Theme.MaterialComponents.DayNight">
        <!-- Theme avec ActionBar native (pas NoActionBar) -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="android:windowBackground">@color/surface</item>
        <item name="android:statusBarColor">@color/surface</item>
        <item name="android:navigationBarColor">@color/surface</item>
        <!-- ActionBar sombre -->
        <item name="actionBarStyle">@style/MyActionBar</item>
        <item name="textInputStyle">@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox</item>
        <!-- Style de bouton par défaut : évite les libellés tronqués -->
        <item name="materialButtonStyle">@style/AppButton</item>
    </style>

    <!--
        Boutons de l'application.
        - textAllCaps=false + letterSpacing=0 : le texte prend beaucoup moins de
          place que le style Material par défaut (MAJUSCULES + espacement large),
          ce qui évite que des libellés comme "Difficile (30 questions)" soient
          coupés dans les boutons à largeur fixe.
        - insetTop/insetBottom=0 : utilise toute la hauteur définie (56dp).
        - Repli automatique de la taille du texte pour les écrans étroits/denses.
    -->
    <style name="AppButton" parent="Widget.MaterialComponents.Button">
        <item name="android:textAllCaps">false</item>
        <item name="android:letterSpacing">0</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:maxLines">1</item>
        <item name="android:textSize">15sp</item>
        <item name="autoSizeTextType">uniform</item>
        <item name="autoSizeMinTextSize">11sp</item>
        <item name="autoSizeMaxTextSize">15sp</item>
    </style>

    <style name="Theme.App_mythology" parent="Base.Theme.App_mythology"/>

    <style name="MyActionBar" parent="Widget.MaterialComponents.ActionBar.Solid">
        <item name="background">@color/surface</item>
        <item name="titleTextStyle">@style/MyActionBarTitle</item>
        <item name="navigationIconTint">@color/primary</item>
    </style>

    <style name="MyActionBarTitle" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textColor">@color/on_surface</item>
    </style>

    <!-- Dialogue plein écran du mode Liste (révélation des informations d'une carte) -->
    <style name="FullScreenDialog" parent="Theme.MaterialComponents.DayNight.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowBackground">@color/surface</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
